# Git Remote Configuration
# This file tracks the Git remote URL and default branch for the project

# Remote URL - Configure with: git remote add origin <URL>
# Recommended: Use GitHub CLI to create repository: gh repo create guitar-hero-teensy-k612-synth --public --source=. --remote=origin
REMOTE_URL=<not_yet_configured>

# Default branch (typically 'main' or 'master')
DEFAULT_BRANCH=main

# Instructions to configure remote:
# 1. Create GitHub repository:
#    gh repo create guitar-hero-teensy-k612-synth --public --source=. --remote=origin --push
#
# 2. Or manually add remote:
#    git remote add origin https://github.com/<username>/guitar-hero-teensy-k612-synth.git
#    git branch -M main
#    git push -u origin main
#
# 3. Update this file with the actual remote URL after configuration
